<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingVertical="7dp"
    android:gravity="center_vertical">

    <!-- Barra con el color real del calendario (se tiñe por código).
         ImageView, no View: los widgets no admiten la clase View genérica. -->
    <ImageView
        android:id="@+id/barra"
        android:layout_width="3dp"
        android:layout_height="34dp"
        android:layout_marginEnd="9dp"
        android:contentDescription="@null"
        android:background="@color/azul" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/titulo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/texto"
            android:textSize="13sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/ubicacion"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/texto_suave"
            android:textSize="11sp"
            android:maxLines="1"
            android:ellipsize="end" />
    </LinearLayout>

    <!-- Doble huso: Asunción arriba, Madrid (ES) debajo -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="end"
        android:paddingStart="8dp">

        <TextView
            android:id="@+id/hora_pri"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/texto"
            android:textSize="15sp" />

        <!-- Misma medida que la hora primaria: ambas a 15sp -->
        <TextView
            android:id="@+id/hora_sec"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/naranja"
            android:textSize="15sp" />
    </LinearLayout>
</LinearLayout>
