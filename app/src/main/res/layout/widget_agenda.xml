<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_fondo"
    android:padding="12dp">

    <!-- Cabecera: fecha de hoy + botón de refresco -->
    <LinearLayout
        android:id="@+id/cabecera"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="8dp">

        <!-- Barras del logo: ImageView porque los widgets no admiten <View> -->
        <ImageView
            android:layout_width="4dp"
            android:layout_height="16dp"
            android:layout_marginEnd="3dp"
            android:contentDescription="@null"
            android:background="@color/azul" />
        <ImageView
            android:layout_width="4dp"
            android:layout_height="16dp"
            android:layout_marginEnd="8dp"
            android:contentDescription="@null"
            android:background="@color/naranja" />

        <TextView
            android:id="@+id/cabecera_fecha"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/texto"
            android:textSize="14sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:text="DualCal" />

        <ImageView
            android:id="@+id/boton_refrescar"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:padding="3dp"
            android:src="@drawable/ic_refrescar"
            android:contentDescription="@string/refrescar" />
    </LinearLayout>

    <!-- Lista de eventos + vista de "sin eventos" -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <ListView
            android:id="@+id/lista"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:dividerHeight="0dp" />

        <TextView
            android:id="@+id/vacio"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:textColor="@color/texto_suave"
            android:textSize="13sp"
            android:text="@string/sin_eventos" />
    </FrameLayout>
</LinearLayout>
