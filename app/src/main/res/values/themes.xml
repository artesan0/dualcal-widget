<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.DualCalWidget" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/fondo</item>
        <item name="android:windowBackground">@color/fondo</item>
    </style>

    <!-- Actividad puente invisible: no debe pintar nada -->
    <style name="Theme.Transparente" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
